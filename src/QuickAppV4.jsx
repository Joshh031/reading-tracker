import { useEffect, useMemo, useState } from 'react';
import { loadCloudState, saveCloudState } from './firebase';

const KEYS={entries:'rt_entries',books:'rt_current_books',library:'rt_library'};
const today=()=>{const d=new Date();return `${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`};
const read=(k,f)=>{try{const v=localStorage.getItem(k);return v?JSON.parse(v):f}catch{return f}};
const write=(k,v)=>{try{localStorage.setItem(k,JSON.stringify(v))}catch{}};
const base=()=>({readingMinutes:0,arabicStudied:false,geoRead:false,dailyTakeaway:'',podcastEntries:[]});
const norm=e=>({...base(),...(e||{}),podcastEntries:Array.isArray(e?.podcastEntries)?e.podcastEntries:[]});
const keyFor=p=>{try{const u=new URL(p.url||'');const eid=u.searchParams.get('i');if(eid)return `apple:${eid}`}catch{}return `${(p.podcast||'').toLowerCase()}|${(p.episodeTitle||'').toLowerCase()}|${(p.guest||'').toLowerCase()}`};
const dedupe=arr=>{const m=new Map();for(const p of arr||[]){const k=keyFor(p);const old=m.get(k);if(!old||((p.investorInsights||[]).length>(old.investorInsights||[]).length))m.set(k,p)}return [...m.values()]};
const meaningful=e=>{const n=norm(e);return n.readingMinutes>=45||n.arabicStudied||n.geoRead||n.podcastEntries.length>0||!!n.dailyTakeaway.trim()};

export default function QuickAppV4(){
  const date=today();
  const [view,setView]=useState('today'),[loaded,setLoaded]=useState(false),[entries,setEntries]=useState({}),[books,setBooks]=useState({physical:'',audible:''}),[library,setLibrary]=useState([]),[link,setLink]=useState(''),[analyzing,setAnalyzing]=useState(false),[refreshing,setRefreshing]=useState(''),[error,setError]=useState(''),[query,setQuery]=useState(''),[sync,setSync]=useState('local');
  useEffect(()=>{const local={entries:read(KEYS.entries,{}),currentBooks:read(KEYS.books,{physical:'',audible:''}),library:read(KEYS.library,[])};(async()=>{let merged=local.entries;try{const cloud=await loadCloudState();if(cloud){merged={...local.entries,...(cloud.entries||{})};if(cloud.currentBooks)local.currentBooks=cloud.currentBooks;if(cloud.library)local.library=cloud.library;setSync('synced')}}catch{setSync('offline')}const cleaned={...merged};for(const k of Object.keys(cleaned)){const e=norm(cleaned[k]);cleaned[k]={...e,podcastEntries:dedupe(e.podcastEntries)}}setEntries(cleaned);setBooks(local.currentBooks);setLibrary(local.library);write(KEYS.entries,cleaned);write(KEYS.books,local.currentBooks);write(KEYS.library,local.library);setLoaded(true)})()},[]);
  const entry=norm(entries[date]);
  async function patch(p){const next={...entry,...p},all={...entries,[date]:next};setEntries(all);write(KEYS.entries,all);setSync('saving');try{await saveCloudState({entries:{[date]:next}});setSync('synced')}catch{setSync('offline')}}
  const updatePodcast=(id,p)=>patch({podcastEntries:entry.podcastEntries.map(x=>x.id===id?{...x,...p}:x)});
  const removePodcast=id=>patch({podcastEntries:entry.podcastEntries.filter(x=>x.id!==id)});
  function makeItem(d,prior={}){return {id:prior.id||`${Date.now()}`,url:d.url||prior.url||'',podcast:d.podcast||'',episodeTitle:d.episodeTitle||'',guest:d.guest||'',guestRole:d.guestRole||'',guestContext:d.guestContext||'',whyGuestMatters:d.whyGuestMatters||'',investorInsights:d.investorInsights||[],oneLine:d.oneLine||'',verified:!!d.verified,verificationNote:d.verificationNote||'',transcriptFound:!!d.transcriptFound,transcriptUrl:d.transcriptUrl||'',summaryBasis:d.summaryBasis||'',insightSource:d.insightSource||'',review:'pending',reviewNote:'',takeaway:prior.takeaway||''}}
  async function fetchEpisode(url){const r=await fetch('/api/podcast',{method:'POST',headers:{'content-type':'application/json'},cache:'no-store',body:JSON.stringify({url})});const d=await r.json();if(!r.ok)throw new Error(d.error||'Could not analyze link');return d}
  async function analyzeLink(){if(!link.trim())return;setAnalyzing(true);setError('');try{const d=await fetchEpisode(link.trim());const probe=makeItem(d);const k=keyFor(probe);const existing=entry.podcastEntries.find(x=>keyFor(x)===k);const item=makeItem(d,existing||{});const others=entry.podcastEntries.filter(x=>keyFor(x)!==k);await patch({podcastEntries:[...others,item]});setLink('')}catch(e){setError(e.message||'Could not analyze this link.')}finally{setAnalyzing(false)}}
  async function refreshPodcast(p){if(!p.url)return;setRefreshing(p.id);setError('');try{const d=await fetchEpisode(p.url);const item=makeItem(d,p);const k=keyFor(item);const others=entry.podcastEntries.filter(x=>x.id===p.id||keyFor(x)!==k).filter(x=>x.id!==p.id);await patch({podcastEntries:[...others,item]})}catch(e){setError(e.message||'Could not refresh takeaways.')}finally{setRefreshing('')}}
  async function saveBooks(next){setBooks(next);write(KEYS.books,next);try{await saveCloudState({currentBooks:next});setSync('synced')}catch{setSync('offline')}}
  const allDates=Object.keys(entries).sort().reverse();
  const streak=useMemo(()=>{let s=0,d=new Date();d.setHours(12,0,0,0);if(!meaningful(entries[date]))d.setDate(d.getDate()-1);while(true){const k=`${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`;if(meaningful(entries[k])){s++;d.setDate(d.getDate()-1)}else break}return s},[entries,date]);
  const results=useMemo(()=>{const q=query.trim().toLowerCase();if(q.length<2)return[];const out=[];for(const d of allDates){const e=norm(entries[d]);for(const p of dedupe(e.podcastEntries)){const hay=[p.podcast,p.episodeTitle,p.guest,p.guestRole,p.guestContext,p.oneLine,p.takeaway,...(p.investorInsights||[])].join(' ').toLowerCase();if(hay.includes(q))out.push({date:d,type:'Podcast',title:p.guest||p.podcast,text:p.takeaway||p.oneLine||p.investorInsights?.[0]||''})}if((e.dailyTakeaway||'').toLowerCase().includes(q))out.push({date:d,type:'Daily insight',title:'Learning day',text:e.dailyTakeaway})}for(const b of library){if([b.title,b.author,b.review].join(' ').toLowerCase().includes(q))out.push({date:b.dateFinished||'',type:'Book',title:b.title,text:b.review||b.author})}return out},[query,entries,library]);
  if(!loaded)return <div className="loading">LOADING…</div>;
  return <div className="app"><style>{`
  @import url('https://fonts.googleapis.com/css2?family=DM+Mono:wght@300;400;500&family=Playfair+Display:wght@600;700&display=swap');*{box-sizing:border-box}html,body,#root{margin:0;min-height:100%;background:#08090b;color:#efeee9}body{font-family:'DM Mono',monospace}.app{max-width:720px;margin:auto;min-height:100vh;padding-bottom:90px}header{padding:24px 18px 14px;border-bottom:1px solid #1f232a;background:linear-gradient(#111319,#08090b)}h1{font:700 30px 'Playfair Display',serif;margin:0}.sub{font-size:10px;color:#737b87;letter-spacing:.16em;margin-top:5px}nav{display:flex;position:sticky;top:0;z-index:4;background:#08090bf2;backdrop-filter:blur(12px);border-bottom:1px solid #1c2026}nav button{flex:1;background:none;border:0;color:#59616c;padding:13px 4px;font:10px 'DM Mono';letter-spacing:.12em}nav button.on{color:#d5b06a;border-bottom:1px solid #d5b06a}main{padding:16px}.card{background:#111319;border:1px solid #222730;border-radius:15px;padding:16px;margin-bottom:12px}.gold{border-color:#4b402c;background:linear-gradient(145deg,#17140e,#111319)}.label{font-size:9px;letter-spacing:.16em;color:#d5b06a;text-transform:uppercase;margin-bottom:8px}.muted{color:#727a85;font-size:11px;line-height:1.55}.title{font:600 19px 'Playfair Display',serif}.guest{font:700 25px 'Playfair Display',serif;margin:2px 0}.role{font-size:12px;color:#d5b06a;line-height:1.45}.row{display:flex;gap:8px}.row>*{flex:1}input,textarea{width:100%;border:1px solid #2b313a;background:#0a0c10;color:#eee;border-radius:10px;padding:11px 12px;font:13px 'DM Mono';outline:none}textarea{resize:vertical}.btn{border:1px solid #343a43;background:#181b21;color:#d7dae0;border-radius:10px;padding:11px 12px;font:10px 'DM Mono';letter-spacing:.08em}.btn.primary{background:#d5b06a;border-color:#d5b06a;color:#121212;font-weight:600}.btn.accept{background:#17351f;border-color:#285a35;color:#91d9a2}.btn.reject{background:#332019;border-color:#59372b;color:#dfaa92}.btn.tiny{padding:7px 9px;font-size:9px}.badge{display:inline-block;font-size:9px;padding:4px 7px;border-radius:20px;background:#20252c;color:#8d96a3;margin:8px 6px 0 0}.badge.good{background:#18301e;color:#86cf96}.context{font:13px Georgia,serif;line-height:1.6;color:#cfd1d4;margin:10px 0}.insights{margin:10px 0 0;padding-left:20px;color:#d4d6da;font-size:12px;line-height:1.72}.insights li{margin-bottom:16px}.review{border-top:1px solid #272b32;margin-top:14px;padding-top:14px}.take{margin-top:6px;border-color:#5b4b30}.episode{font-size:10px;color:#59616a;margin-top:10px;line-height:1.45}.minutes{display:flex;gap:6px;flex-wrap:wrap}.minutes button.on{background:#314c38;border-color:#42624a;color:#86cf96}.toggle{display:flex;justify-content:space-between;align-items:center}.switch{width:44px;height:25px;border-radius:20px;background:#242a32;position:relative}.switch:after{content:'';position:absolute;top:3px;left:3px;width:19px;height:19px;border-radius:50%;background:#777}.switch.on{background:#314c38}.switch.on:after{left:22px;background:#86cf96}.status{font-size:9px;color:#69717c}.status.synced{color:#86cf96}.big{font:700 27px 'Playfair Display',serif}.result{padding:12px 2px;border-bottom:1px solid #20242a}.loading{min-height:100vh;display:grid;place-items:center;background:#08090b;color:#666}@media(max-width:560px){.stack{display:block}.stack>*{margin-bottom:8px}}
  `}</style>
  <header><div className="row" style={{alignItems:'end'}}><div><h1>Reading Log</h1><div className="sub">CAPTURE WHAT MATTERS · IN UNDER 4 MINUTES</div></div><div style={{textAlign:'right'}}><div className="big">{streak}</div><div className="muted">day streak</div><div className={`status ${sync}`}>● {sync.toUpperCase()}</div></div></div></header>
  <nav>{[['today','TODAY'],['search','SEARCH'],['books','BOOKS']].map(([k,l])=><button key={k} className={view===k?'on':''} onClick={()=>setView(k)}>{l}</button>)}</nav><main>
  {view==='today'&&<><div className="card gold"><div className="label">Fast podcast capture</div><div className="title">Paste the episode link</div><div className="muted" style={{margin:'5px 0 12px'}}>One episode, one card. Re-pasting refreshes it instead of creating a duplicate.</div><div className="row stack"><input value={link} onChange={e=>setLink(e.target.value)} onKeyDown={e=>e.key==='Enter'&&analyzeLink()} placeholder="Apple Podcasts episode link…"/><button className="btn primary" disabled={analyzing} onClick={analyzeLink}>{analyzing?'ANALYZING…':'AUTOFILL'}</button></div>{error&&<div style={{color:'#d98686',fontSize:11,marginTop:9}}>{error}</div>}</div>
  {dedupe(entry.podcastEntries).map(p=><div className="card" key={p.id}><div className="row" style={{alignItems:'start'}}><div><div className="label">🎙 {p.podcast||'Podcast'}</div><div className="guest">{p.guest||'Guest not identified'}</div>{p.guestRole&&<div className="role">{p.guestRole}</div>}</div><button className="btn tiny" style={{flex:'0 0 auto'}} onClick={()=>removePodcast(p.id)}>REMOVE</button></div><div><span className="badge good">✓ EXACT EPISODE VERIFIED</span>{p.transcriptFound&&<span className="badge good">✓ TRANSCRIPT/SUMMARY</span>}{p.insightSource&&<span className="badge">{p.insightSource}</span>}</div>{p.guestContext&&<><div className="label" style={{marginTop:14}}>Why listen to this guest</div><div className="context">{p.guestContext}</div></>}<div className="label" style={{marginTop:16}}>PM-worthy insights</div>{p.investorInsights?.length?<ul className="insights">{p.investorInsights.map((x,i)=><li key={i}>{x}</li>)}</ul>:<div className="muted">No sufficiently supported takeaways were found.</div>}<button className="btn" style={{marginTop:8}} disabled={refreshing===p.id} onClick={()=>refreshPodcast(p)}>{refreshing===p.id?'REFRESHING…':'↻ REFRESH TAKEAWAYS'}</button>{p.investorInsights?.length>0&&<div className="review"><div className="label">Are these takeaways useful?</div><div className="row"><button className="btn accept" onClick={()=>updatePodcast(p.id,{review:'accepted',reviewNote:''})}>✓ ACCEPT</button><button className="btn reject" onClick={()=>updatePodcast(p.id,{review:'needs_work'})}>✕ NEEDS WORK</button></div>{p.review==='needs_work'&&<textarea rows={2} style={{marginTop:9}} placeholder="What did it miss or get wrong?" value={p.reviewNote||''} onChange={e=>updatePodcast(p.id,{reviewNote:e.target.value})}/>}</div>}<div className="label" style={{marginTop:16}}>My most important takeaway</div><textarea className="take" rows={3} placeholder="One sentence in your own words…" value={p.takeaway||''} onChange={e=>updatePodcast(p.id,{takeaway:e.target.value})}/><div className="episode">{p.episodeTitle||'Episode title unavailable'}</div></div>)}
  <div className="card"><div className="label">📖 Reading</div><div className="row" style={{marginBottom:10}}><div><div className="muted">Physical</div><div className="title">{books.physical||'Not set'}</div></div><div><div className="muted">Audible</div><div className="title">{books.audible||'Not set'}</div></div></div><div className="minutes">{[0,15,30,45,60,90].map(m=><button key={m} className={`btn ${entry.readingMinutes===m?'on':''}`} onClick={()=>patch({readingMinutes:m})}>{m===0?'NONE':`${m} MIN`}</button>)}</div></div><div className="row"><div className="card toggle" onClick={()=>patch({arabicStudied:!entry.arabicStudied})}><div><div className="label">🌙 Arabic</div><div className="muted">{entry.arabicStudied?'Done today':'Tap when done'}</div></div><div className={`switch ${entry.arabicStudied?'on':''}`}/></div><div className="card toggle" onClick={()=>patch({geoRead:!entry.geoRead})}><div><div className="label">🌐 Geopol</div><div className="muted">{entry.geoRead?'Read today':'Tap when read'}</div></div><div className={`switch ${entry.geoRead?'on':''}`}/></div></div><div className="card"><div className="label">🧠 One thing worth remembering today</div><textarea rows={3} placeholder="Optional. One sentence is enough." value={entry.dailyTakeaway||''} onChange={e=>patch({dailyTakeaway:e.target.value})}/></div></>}
  {view==='search'&&<div className="card"><div className="label">Search your knowledge</div><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="guest, company, thesis…"/>{results.map((r,i)=><div className="result" key={i}><div className="label">{r.type} · {r.date}</div><div className="title">{r.title}</div><div className="muted">{r.text}</div></div>)}</div>}
  {view==='books'&&<><div className="card"><div className="label">Current books</div><div className="muted">Physical</div><input value={books.physical||''} onChange={e=>saveBooks({...books,physical:e.target.value})} placeholder="Current physical book"/><div className="muted" style={{marginTop:10}}>Audible</div><input value={books.audible||''} onChange={e=>saveBooks({...books,audible:e.target.value})} placeholder="Current audiobook"/></div>{library.map((b,i)=><div className="card" key={i}><div className="title">{b.title}</div><div className="muted">{b.author}</div></div>)}</>}
  </main></div>
}
